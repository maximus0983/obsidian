[[Generics]]


Не ис­пользуйте ограниченные типы с символами подстановки в качестве воз­вращаемых типов. Вместо дополнительной гибкости для ваших пользовате­лей это заставит их использовать типы с символами подстановки в клиентском коде


wildcard нельзя унаследовать сразу от нескольких интерфейсов в границе.

для Type parameter вроде нельзя super поставить в границе, а для wildcard можно.

к wildcard внутри метода уже никак нельзя обратиться именно как к типу.  он там уже не фигурирует