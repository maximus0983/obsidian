![[Снимок экрана 2026-01-11 в 19.17.51.png]]

variable: PromptValue | str | Sequence[MessageLikeRepresentation]
так тоже можно. это значит переменная может быть из этих типов. автоматом питон не валидирует, но в инфе про аннотации сохраняется и можно логику писать на этой основе. 
pydantic может сразу с ошибкой выкинуть когда задашь не тот тип